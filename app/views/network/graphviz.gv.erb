digraph network {
<% @nodes.each do |node| -%>
	<%= node.id %> [label="<%= node.location.name %>/<%= node.device.device_type.desc %>"];
<% node.children.each do |child| -%>
	<%= node.id %> -> <%= child.id %>;
<% end -%>
<% end -%>
}